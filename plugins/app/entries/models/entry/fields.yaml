# ===================================
#  Form Field Definitions
# ===================================

fields:
    task_id:
        label: Task id
        type: number
    time_start:
        label: Time from
        type: timestamp
    time_end:
        label: Time to
        type: timestamp
    tracked_time:
        label: tracked_time
        type: string
